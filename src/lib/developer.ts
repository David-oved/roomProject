/**
 * ═══════════════════════════════════════════════════════════════
 *  חשבון המפתח
 * ═══════════════════════════════════════════════════════════════
 *  קבוע יחיד, לא רשימה ולא תפקיד — כי זה בדיוק מה שהתבקש: מסך שרק
 *  בעל האפליקציה רואה, לא "כל מנהל", ולא משהו שאפשר להעניק בטעות
 *  למישהו אחר. "מנהל חדר" הוא כבר תפקיד קיים שכל אחד יכול לקבל
 *  (יוצר החדר) — זה לא זה.
 *
 *  ‼️ הבדיקה כאן היא נוחות תצוגה בלבד — היא מחליטה אם *להראות* את
 *     הטאב, לא אם מותר *לקרוא* את הנתונים. האכיפה האמיתית יושבת ב-
 *     database.rules.json, שמשווה את auth.token.email לאותו קבוע
 *     בדיוק בצד השרת. גם מי שיפתח את ה-URL ישירות נחסם שם, לפני
 *     שהוא רואה שורת מידע אחת — ראו docs/14-developer-panel.md.
 * ═══════════════════════════════════════════════════════════════
 */
export const DEVELOPER_EMAIL = 'wbddwd55@gmail.com';

export function isDeveloper(email: string | null | undefined): boolean {
  return email === DEVELOPER_EMAIL;
}
