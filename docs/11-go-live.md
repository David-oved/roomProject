# 11 · מצב הפעלה

**עודכן: 18.8.2026** · https://david-oved.github.io/roomProject/ · גרסה `1.0.3`

---

## תשתית — הכל ירוק ✅

| בדיקה | תוצאה | איך נבדק |
|-------|-------|----------|
| התחברות אימייל/סיסמה מופעלת | ✅ | `INVALID_LOGIN_CREDENTIALS` (ולא `PASSWORD_LOGIN_DISABLED`) |
| `david-oved.github.io` מורשה | ✅ | מופיע ב-`authorizedDomains` |
| Realtime Database קיים | ✅ | `europe-west1`, מגיב |
| כללי אבטחה אוכפים | ✅ | גישה אנונימית ל-`/rooms` → HTTP 401 |
| תחביר הכללים | ✅ | אומת ע"י Firebase, 125 הצהרות |
| האפליקציה נטענת | ✅ | מסך התחברות מוצג, ללא באנר "אין חיבור" |
| שרשרת האימות | ✅ | ניסיון כושל מכוון → שגיאה נכונה בעברית |
| **מנגנון העדכון** | ✅ | 1.0.2 → זוהה 1.0.3 → באנר → עדכון → נטען מחדש כ-1.0.3 |
| חישובי כספים | ✅ | 39 בדיקות יחידה |
| TypeScript | ✅ | ללא שגיאות |
| פריסה אוטומטית | ✅ | 6 ריצות מוצלחות |

---

## ⚠️ מה שעדיין לא נבדק — וצריך אותך

הזרימות שדורשות חשבון מחובר **לא הורצו מעולם**:

הרשמה · יצירת חדר · הצטרפות · אישור מנהל · דיווח מוצר · סנכרון בזמן אמת ·
"אני קונה" · רישום קנייה · חלוקת הוצאות · אישור קנייה · עדכון מאזנים

**למה לא בדקתי:** יצירת חשבונות והזנת סיסמאות הן פעולות שאני לא מבצע —
גם כשיש לי גישה טכנית. זו בדיקה שחייבת להיות שלך.

הלוגיקה הכספית מכוסה ב-39 בדיקות יחידה וכללי האבטחה נפרסו ואומתו,
אבל **המסלול המלא בין שני משתמשים אמיתיים טרם רץ אפילו פעם אחת.**

---

## סקריפט קבלה — 3 דקות

### שלב 1 · חשבון ראשון
1. פתח https://david-oved.github.io/roomProject/
2. `הרשמה` ← שם, אימייל, סיסמה (6+ תווים)

   ⚠️ **אל תשתמש ב-`wbddwd55@gmail.com`** — הוא תפוס ע"י חשבון גוגל קיים.
   אם בכל זאת תנסה, תקבל הודעה עם כפתור "הגדרת סיסמה חדשה" שפותר את זה.

3. `יצירת חדר חדש` ← שם ייחודי ← **העתק את הקוד בן 6 התווים**

### שלב 2 · חשבון שני (חלון פרטי)
4. `Ctrl+Shift+N` ← אותה כתובת ← הרשמה עם אימייל אחר
5. `הצטרפות לחדר קיים` ← הזן את הקוד ← שלח

### שלב 3 · הדברים שחייבים לעבוד
6. בחלון הראשון: הבקשה מופיעה **תוך שנייה** ← `אישור`
7. החלון השני נכנס לחדר **אוטומטית**
8. דווח על מוצר בחלון אחד → מופיע בשני **תוך שנייה** ← זה מבחן הסנכרון
9. בחלון השני: `אני קונה את זה` ← `קניתי` ← סכום `20` ← שמור
10. בחלון הראשון: `חשבון` ← `ממתינות` ← `אישור`
11. **המאזנים:** אחד `₪10.00-`, השני `₪10.00` ← זה מבחן הכסף

אם 8 ו-11 עובדים — הליבה של האפליקציה תקינה.

---

## אם משהו נראה תקוע על גרסה ישנה

סגור את כל הטאבים של האתר ופתח מחדש. הדפדפן מחליף Service Worker רק
כשאין אף טאב פתוח.

מגרסה 1.0.2 זה כבר לא אמור לקרות: כל פריסה מזוהה אוטומטית לפי מזהה
בנייה, וגם אם האפליקציה נשברת לגמרי — ה-HTML עצמו מנקה ומרענן אחרי
10 שניות.
